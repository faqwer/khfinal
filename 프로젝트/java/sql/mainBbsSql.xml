<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="finaltp.mainBbs">
	<select id="noticeList" resultType="finaltp.mainBbs.model.MainBbsDTO">
		<![CDATA[
		SELECT * FROM
			(SELECT ROWNUM AS rnum, a.* FROM
			(SELECT * FROM total_bbs WHERE category = 'notice' ORDER BY bbs_idx desc)a)b
			WHERE rnum >= #{startnum} AND rnum <= #{endnum}
		]]>
	</select>
	<select id="totalCnt" resultType="Integer">
		SELECT count(*) FROM total_bbs WHERE category = 'notice'
	</select>
	
	<select id="noticeContent" parameterType="Integer" resultType="finaltp.mainBbs.model.MainBbsDTO">
		SELECT  * FROM total_bbs WHERE bbs_idx = #{idx}
	</select>
	
	<select id="memberIdx" parameterType="String" resultType="Integer">
		SELECT member_idx FROM plan_member WHERE id = #{userid}
	</select>
	
	<select id="bbsMemberIdx" parameterType="Integer" resultType="Integer">
		SELECT member_idx FROM total_bbs WHERE bbs_idx = #{bbs_idx}
	</select>
	
	<insert id="accWrite1" parameterType="finaltp.mainBbs.model.MainBbsDTO">
		INSERT INTO total_bbs VALUES(Total_bbs_bbs_idx.nextval, #{member_idx}, #{category}, #{status}, 0, #{content}, #{ref}, #{sunbun}, #{lev}, SYSDATE)
	</insert>
	
	<select id="accList1" resultType="finaltp.mainBbs.model.MainBbsDTO">
		<![CDATA[
		SELECT * FROM
			(SELECT ROWNUM AS rnum, a.* FROM
			(SELECT * FROM total_bbs WHERE category = 'acc' ORDER BY bbs_idx desc)a)b
			WHERE rnum >= #{startnum} AND rnum <= #{endnum}
		]]>
	</select>
	
	<select id="getUserId" parameterType="Integer" resultType="String">
		SELECT id FROM plan_member WHERE member_idx = #{member_idx}
	</select>
	
</mapper>